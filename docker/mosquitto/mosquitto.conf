# Local demo broker only — anonymous access is intentionally allowed here.
# Do not reuse this config for anything internet-facing; see README's
# "Limitations" section.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
